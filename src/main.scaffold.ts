/**
 * Scaffold apenas — não é compilado pelo Next.
 * Implementação real do scanner:
 *   - src/app/actions/case-actions.ts (auditCaseCoreSystem)
 *   - src/app/api/datajud-worker/route.ts
 */
export const NEST_WORKER_SCAFFOLD = true;
